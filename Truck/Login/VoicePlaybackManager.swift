import UIKit
import AVFoundation
enum PlayVoiceType {
    case loadPoint
    case unloadPoint
    case newNotification
}
class VoicePlaybackManager {
    static func playWithType(_ type: PlayVoiceType) {
        
    }
}
